import * as THREE from 'https://cdn.jsdelivr.net/npm/three@0.160.0/build/three.module.js';

const $ = (s) => document.querySelector(s);
const VERSION = 'Beta 0.1.9';
const isForcedLandscape=()=>matchMedia('(orientation:portrait) and (pointer:coarse)').matches;
const viewport=()=>isForcedLandscape()?{w:innerHeight,h:innerWidth}:{w:innerWidth,h:innerHeight};

function showFatal(error) {
  console.error('[Cidade Viva] fatal boot error:', error);
  const loading = $('#loading-screen');
  if (!loading) return;
  loading.classList.remove('done');
  loading.innerHTML = `<div class="loading-brand">CIDADE VIVA</div><div class="loading-version">${VERSION}</div><div class="loading-tip" style="max-width:520px;text-align:center">FALHA AO INICIAR<br><small>${String(error?.message || error)}</small></div>`;
}

try {
  const game = $('#game');
  if (!game) throw new Error('Elemento #game não encontrado');
  const vp=viewport();
  const scene = new THREE.Scene();
  scene.background = new THREE.Color(0x93b9cf);
  scene.fog = new THREE.Fog(0x93b9cf,145,335);
  const camera = new THREE.PerspectiveCamera(62,vp.w/vp.h,.05,600);
  const renderer = new THREE.WebGLRenderer({antialias:true,powerPreference:'high-performance'});
  renderer.setSize(vp.w,vp.h,false);
  renderer.setPixelRatio(Math.min(devicePixelRatio,1.35));
  renderer.outputColorSpace = THREE.SRGBColorSpace;
  renderer.shadowMap.enabled = true;
  renderer.shadowMap.type = THREE.PCFSoftShadowMap;
  game.appendChild(renderer.domElement);
  scene.add(new THREE.HemisphereLight(0xeaf7ff,0x5c6b55,1.45));
  const sun = new THREE.DirectionalLight(0xfff2db,2);sun.position.set(50,80,30);sun.castShadow=true;sun.shadow.mapSize.set(1024,1024);scene.add(sun);
  const mat=(c,r=.9,m=0)=>new THREE.MeshStandardMaterial({color:c,roughness:r,metalness:m});
  const roadMat=mat(0x272d31,.98),sideMat=mat(0xb9bab3,.98),grassMat=mat(0x557a4e,1),lineMat=mat(0xead04a,.8),whiteMat=mat(0xf2f1e9,.82),darkMat=mat(0x171b20,.7,.1);
  function box(w,h,d,material,x=0,y=0,z=0){const o=new THREE.Mesh(new THREE.BoxGeometry(w,h,d),material);o.position.set(x,y,z);o.castShadow=true;o.receiveShadow=true;return o;}
  const roads=[-60,0,60],blocks=[-90,-30,30,90],statics=[],trees=[],poles=[],signals=[],cars=[],npcs=[],towJobs=[],samuJobs=[],projectiles=[];
  let activeTowJob=null,activeSamuJob=null;
  function addStatic(x,z,hx,hz,type,owner=null){const c={x,z,hx,hz,type,owner,active:true};statics.push(c);return c;}
  function circleAabb(p,r,b){if(!b.active)return false;const x=THREE.MathUtils.clamp(p.x,b.x-b.hx,b.x+b.hx),z=THREE.MathUtils.clamp(p.z,b.z-b.hz,b.z+b.hz),dx=p.x-x,dz=p.z-z;return dx*dx+dz*dz<r*r;}
  function hitsStatic(p,r=.4){return statics.find(b=>circleAabb(p,r,b))||null;}
  function localCar(c,p){const q=p.clone().sub(c.g.position),a=-c.g.rotation.y,co=Math.cos(a),si=Math.sin(a);return{x:q.x*co-q.z*si,z:q.x*si+q.z*co};}
  function hitsCar(c,p,r=.4){if(!c||c.removed)return false;const q=localCar(c,p);return Math.abs(q.x)<=c.halfW+r&&Math.abs(q.z)<=c.halfL+r;}
  function carsTouch(a,b){if(!a||!b||a.removed||b.removed)return false;const pts=[[0,a.halfL],[0,-a.halfL],[a.halfW,a.halfL*.72],[-a.halfW,a.halfL*.72],[a.halfW,-a.halfL*.72],[-a.halfW,-a.halfL*.72]];return pts.some(([x,z])=>hitsCar(b,a.g.localToWorld(new THREE.Vector3(x,0,z)),.08));}
  function carStaticHit(c){for(const [x,z] of [[0,c.halfL],[0,-c.halfL],[c.halfW,c.halfL*.72],[-c.halfW,c.halfL*.72],[c.halfW,-c.halfL*.72],[-c.halfW,-c.halfL*.72]]){const h=hitsStatic(c.g.localToWorld(new THREE.Vector3(x,0,z)),.16);if(h)return h;}return null;}
  function person(color=0x20242a,skin=0xd2a27e){const g=new THREE.Group(),torso=box(.72,1.08,.4,mat(color),0,1.22,0),head=new THREE.Mesh(new THREE.SphereGeometry(.29,14,10),mat(skin)),ll=box(.25,.82,.27,mat(0x202733),-.19,.43,0),rl=box(.25,.82,.27,mat(0x202733),.19,.43,0),la=box(.19,.9,.2,mat(color),-.46,1.25,0),ra=box(.19,.9,.2,mat(color),.46,1.25,0);head.position.y=2;g.add(torso,head,ll,rl,la,ra);g.userData.parts={torso,head,ll,rl,la,ra};return g;}
  const LIGHT={GREEN:8,YELLOW:2,RED:8,CYCLE:18};
  function addTree(x,z){const g=new THREE.Group();g.add(box(.36,2.2,.36,mat(0x593d25),0,1.2,0));const crown=new THREE.Mesh(new THREE.SphereGeometry(1.15,10,8),mat(0x315f34));crown.position.y=3.15;g.add(crown);g.position.set(x,0,z);scene.add(g);const t={g,fallen:false,collider:null};t.collider=addStatic(x,z,.3,.3,'tree',t);trees.push(t);}
  function addSignal(x,z,rot=0,phase=0){const g=new THREE.Group();g.add(box(.2,3.25,.2,darkMat,0,1.62,0),box(2.5,.14,.14,darkMat,1.15,3.05,0),box(.52,1.35,.44,darkMat,2.2,2.72,0));const mk=(y,c)=>{const s=new THREE.Mesh(new THREE.SphereGeometry(.115,10,8),new THREE.MeshStandardMaterial({color:c,emissive:c,emissiveIntensity:.08}));s.position.set(2.2,y,-.24);g.add(s);return s;};const red=mk(3.08,0x6a1010),yellow=mk(2.72,0x725d10),green=mk(2.36,0x0f5f27);g.position.set(x,.12,z);g.rotation.y=rot;scene.add(g);const s={g,red,yellow,green,phase,fallen:false,state:'RED',collider:null};s.collider=addStatic(x,z,.3,.3,'pole',s);signals.push(s);poles.push(s);return s;}
  function signalState(s,t){const p=(t+s.phase)%LIGHT.CYCLE;if(p<LIGHT.GREEN)return'GREEN';if(p<LIGHT.GREEN+LIGHT.YELLOW)return'YELLOW';return'RED';}
  function updateSignals(t){for(const s of signals){if(s.fallen)continue;s.state=signalState(s,t);const set=(m,on,c)=>{m.material.color.setHex(on?c:0x171717);m.material.emissive.setHex(on?c:0);m.material.emissiveIntensity=on?1.8:.02;};set(s.green,s.state==='GREEN',0x25d45c);set(s.yellow,s.state==='YELLOW',0xffbf28);set(s.red,s.state==='RED',0xff3030);}}
  function signalAhead(vehicle){const pos=vehicle.g.position,f=new THREE.Vector3(Math.sin(vehicle.g.rotation.y),0,Math.cos(vehicle.g.rotation.y));for(const s of signals){if(s.fallen)continue;const d=s.g.position.clone().sub(pos).setY(0),dist=d.length();if(dist>13||dist<1.5)continue;if(f.dot(d.normalize())<.78)continue;if(s.state!=='GREEN')return true;}return false;}
  function knock(o,car){if(!o||o.fallen)return;o.fallen=true;if(o.collider)o.collider.active=false;const d=o.g.position.clone().sub(car.g.position);o.axis=Math.abs(d.x)>Math.abs(d.z)?'z':'x';o.sign=(o.axis==='z'?d.x:d.z)>=0?1:-1;}
  function world(){const ground=new THREE.Mesh(new THREE.PlaneGeometry(250,250),grassMat);ground.rotation.x=-Math.PI/2;ground.position.y=-.06;scene.add(ground);for(const x of roads){const r=new THREE.Mesh(new THREE.PlaneGeometry(18,240),roadMat);r.rotation.x=-Math.PI/2;r.position.set(x,0,0);scene.add(r);}for(const z of roads){const r=new THREE.Mesh(new THREE.PlaneGeometry(240,18),roadMat);r.rotation.x=-Math.PI/2;r.position.set(0,.002,z);scene.add(r);}for(const x of roads)for(let z=-110;z<=110;z+=10)if(!roads.some(v=>Math.abs(z-v)<11))scene.add(box(.14,.018,4.2,lineMat,x,.022,z));for(const z of roads)for(let x=-110;x<=110;x+=10)if(!roads.some(v=>Math.abs(x-v)<11))scene.add(box(4.2,.018,.14,lineMat,x,.024,z));for(const x of roads)for(const z of roads){for(let i=-3;i<=3;i++){scene.add(box(.66,.02,7.4,whiteMat,x+i*1.15,.028,z-12));scene.add(box(.66,.02,7.4,whiteMat,x+i*1.15,.028,z+12));scene.add(box(7.4,.02,.66,whiteMat,x-12,.028,z+i*1.15));scene.add(box(7.4,.02,.66,whiteMat,x+12,.028,z+i*1.15));}addSignal(x-13,z-13,0,0);addSignal(x+13,z+13,Math.PI,0);addSignal(x-13,z+13,-Math.PI/2,9);addSignal(x+13,z-13,Math.PI/2,9);}const cols=[0x93836f,0x718087,0xa99d78,0x806b62,0x9aa2a5];for(const bx of blocks)for(const bz of blocks){scene.add(box(42,.18,42,sideMat,bx,.08,bz));for(let i=0;i<2+Math.floor(Math.random()*3);i++){const w=11+Math.random()*7,d=11+Math.random()*7,h=12+Math.random()*28,x=bx+(Math.random()-.5)*18,z=bz+(Math.random()-.5)*18;scene.add(box(w,h,d,mat(cols[Math.floor(Math.random()*cols.length)],.94),x,h/2+.18,z));addStatic(x,z,w/2+.2,d/2+.2,'building');}for(const [ox,oz] of [[-18,-8],[-18,8],[18,-8],[18,8],[-8,-18],[8,-18],[-8,18],[8,18]])if(Math.random()<.45)addTree(bx+ox,bz+oz);}scene.add(box(30,.16,18,mat(0x6a6e70),-82,.02,-116));scene.add(box(18,5,10,mat(0x4d555a),-88,2.5,-121));scene.add(box(8,.12,4,lineMat,-70,.08,-112));scene.add(box(28,.16,18,mat(0xdedede),82,.02,116));scene.add(box(17,5,10,mat(0xf4f4f4),88,2.5,121));scene.add(box(5,.16,1.2,mat(0xc93030),88,4.2,115));}
  world();
  function carMesh(color,kind='normal'){const tow=kind==='tow',amb=kind==='samu',g=new THREE.Group(),body=box(tow?2.25:1.9,.65,tow?5:4,mat(color,.55,.15),0,.68,0),hood=box(tow?2.05:1.75,.36,1.25,mat(color,.55,.15),0,1,1.35),cab=box(tow?1.95:1.68,.8,tow?1.9:1.72,mat(0x7b95a3,.28,.06),0,1.3,-.35);g.add(body,hood,cab);if(tow){g.add(box(1.9,.16,2.6,darkMat,0,.78,-2.15));g.add(box(.18,.18,2.2,lineMat,0,.92,-2.25));}if(amb){g.add(box(1.5,.14,.35,mat(0xc93030),0,1.72,-.2));g.add(box(.32,.16,1.2,mat(0x2468a2),0,1.84,-.2));}for(const sx of[-1,1])for(const sz of[-1,1]){const w=new THREE.Mesh(new THREE.CylinderGeometry(.37,.37,.26,12),darkMat);w.rotation.z=Math.PI/2;w.position.set(sx*(tow?1.13:.98),.4,sz*(tow?1.58:1.24));g.add(w);}return{g,body,hood,cab};}
  function attachDriver(car,color=0x36556f){if(car.driver)return car.driver;const d=person(color,0xb98262);d.scale.setScalar(.55);d.position.set(0,.62,-.18);d.rotation.y=Math.PI;d.userData.hp=100;d.userData.vehicle=car;d.userData.color=color;car.g.add(d);car.driver=d;return d;}
  function separationPoint(car,pos,extra=.75){const q=localCar(car,pos),dx=car.halfW-Math.abs(q.x),dz=car.halfL-Math.abs(q.z);let lx=q.x,lz=q.z;if(dx<dz)lx=(q.x>=0?1:-1)*(car.halfW+extra);else lz=(q.z>=0?1:-1)*(car.halfL+extra);const p=car.g.localToWorld(new THREE.Vector3(lx,.18,lz));p.y=.18;return p;}

  function sidewalkPointNear(origin,radius=16){
    const tries=[];for(const bx of blocks)for(const bz of blocks){for(const [ox,oz] of [[18,0],[-18,0],[0,18],[0,-18]]){const p=new THREE.Vector3(bx+ox,.18,bz+oz);const d=p.distanceTo(origin);if(d<radius)tries.push({p,d});}}
    tries.sort((a,b)=>a.d-b.d);for(const t of tries){const p=t.p.clone();if(!hitsStatic(p,.42)&&cars.every(c=>!c.removed&&!hitsCar(c,p,.45)||c.removed))return p;}return null;
  }
  function isRoadPoint(p){return roads.some(r=>Math.abs(p.x-r)<8.7||Math.abs(p.z-r)<8.7);}
  function vehicleThreatFor(npc){let best=null;for(const c of cars){if(c.removed||c.exploded||Math.abs(c.speed)<2)continue;const d=npc.g.position.clone().sub(c.g.position).setY(0),dist=d.length();if(dist>10||dist<.1)continue;const f=new THREE.Vector3(Math.sin(c.g.rotation.y),0,Math.cos(c.g.rotation.y));if(f.dot(d.clone().normalize())>.55&&(!best||dist<best.dist))best={car:c,dist,dir:d.normalize()};}return best;}

  class NPC{
    constructor(name,i){
      this.name=name;this.g=person([0x7b3d55,0x376b8b,0x4f7a48,0x8a642f][i%4],[0xd2a27e,0x8d5c45,0xe0b18c][i%3]);this.g.traverse(o=>o.userData.npc=this);scene.add(this.g);
      this.hp=100;this.down=0;this.vel=new THREE.Vector3();this.target=null;this.samuRequested=false;this.speed=1.25;this.ignoreCar=null;this.ignoreCarTimer=0;
      this.brain={state:'wander',fear:.15+Math.random()*.5,courage:.15+Math.random()*.7,curiosity:.2+Math.random()*.7,social:.2+Math.random()*.7,think:.2+Math.random()*.5,alert:0,focus:null,memories:[],lastDecision:0};
      this.relocate();
    }
    remember(type,pos,ttl=10,weight=1){const b=this.brain,m={type,pos:pos.clone(),ttl,weight,time:clock?.elapsedTime||0};b.memories.unshift(m);if(b.memories.length>8)b.memories.length=8;return m;}
    forget(dt){for(const m of this.brain.memories)m.ttl-=dt;this.brain.memories=this.brain.memories.filter(m=>m.ttl>0);this.brain.alert=Math.max(0,this.brain.alert-dt*.08);}
    relocate(){for(let k=0;k<60;k++){const bx=blocks[Math.floor(Math.random()*4)],bz=blocks[Math.floor(Math.random()*4)],edge=[[18,(Math.random()-.5)*32],[-18,(Math.random()-.5)*32],[(Math.random()-.5)*32,18],[(Math.random()-.5)*32,-18]][Math.floor(Math.random()*4)],p=new THREE.Vector3(bx+edge[0],.18,bz+edge[1]),onRoad=isRoadPoint(p);if(!onRoad&&!hitsStatic(p,.4)&&cars.every(c=>!hitsCar(c,p,.45))){this.g.position.copy(p);return;}}this.g.position.set(18,.18,18);}
    hit(dmg,region='body'){if(this.hp<=0)return;this.remember('attacked',player.position,14,2);this.brain.alert=1;this.brain.state='flee';this.hp=Math.max(0,this.hp-dmg);if(this.hp<=0){this.down=999;this.g.rotation.z=Math.PI/2;return;}this.down=Math.max(this.down,region==='head'?0:3.2);this.g.rotation.z=Math.PI/2;if(this.hp<100)requestSamu(this);}
    impact(speed,dir,sourceCar=null){if(this.hp<=0||speed<2)return;let dmg=0,push=.25,down=1.2;if(speed<4.5){dmg=4+(speed-2)*2;push=.65;down=1.4;}else if(speed<8){dmg=12+(speed-4.5)*5;push=2.8;down=2.4;}else{dmg=Math.min(90,30+(speed-8)*7);push=5.2;down=3.5;}if(sourceCar){this.ignoreCar=sourceCar;this.ignoreCarTimer=.8;this.remember('vehicle-danger',sourceCar.g.position,8,1.7);this.brain.alert=1;const sep=separationPoint(sourceCar,this.g.position,.62);if(!hitsStatic(sep,.32))this.g.position.copy(sep);}this.hit(Math.round(dmg));this.down=Math.max(this.down,down);this.vel.copy(dir).multiplyScalar(push);this.vel.y=speed>=8?2.2+speed*.12:0;}
    chooseWander(){const bx=blocks[Math.floor(Math.random()*4)],bz=blocks[Math.floor(Math.random()*4)],opts=[[18,(Math.random()-.5)*30],[-18,(Math.random()-.5)*30],[(Math.random()-.5)*30,18],[(Math.random()-.5)*30,-18]],o=opts[Math.floor(Math.random()*opts.length)];this.target=new THREE.Vector3(bx+o[0],.18,bz+o[1]);this.brain.state='wander';}
    fleeFrom(pos,boost=1){const away=this.g.position.clone().sub(pos).setY(0);if(away.lengthSq()<.01)away.set(Math.random()-.5,0,Math.random()-.5);away.normalize();let p=this.g.position.clone().addScaledVector(away,12+8*boost);if(isRoadPoint(p)||hitsStatic(p,.4)){const safe=sidewalkPointNear(this.g.position,28);if(safe)p=safe;}this.target=p;this.brain.state='flee';this.brain.alert=Math.max(this.brain.alert,.7);}
    think(){
      const b=this.brain;if(this.hp<=0||this.down>0)return;
      const vt=vehicleThreatFor(this);if(vt){this.remember('car-coming',vt.car.g.position,4,2);const side=new THREE.Vector3(-vt.dir.z,0,vt.dir.x);if(Math.random()<.5)side.multiplyScalar(-1);this.target=this.g.position.clone().addScaledVector(side,8);if(isRoadPoint(this.target)){const safe=sidewalkPointNear(this.g.position,20);if(safe)this.target=safe;}b.state='dodge';b.alert=1;return;}
      const recentShot=clock.elapsedTime-lastShot<1.1&&this.g.position.distanceTo(player.position)<22;if(recentShot){this.remember('gunshot',player.position,9,2);this.fleeFrom(player.position,1.2);return;}
      let danger=null;for(const c of cars){if(c.removed)continue;const d=this.g.position.distanceTo(c.g.position);if((c.critical&&d<14)||(c.exploded&&d<18)){danger={pos:c.g.position,d};break;}}if(danger){this.remember('explosion-risk',danger.pos,12,2);this.fleeFrom(danger.pos,1.4);return;}
      const memory=b.memories.find(m=>m.weight>=1.5);if(memory&&b.fear>.28){this.fleeFrom(memory.pos,.7);return;}
      const hurt=npcs.filter(n=>n!==this&&n.hp>0&&n.down>0&&this.g.position.distanceTo(n.g.position)<10).sort((a,c)=>this.g.position.distanceTo(a.g.position)-this.g.position.distanceTo(c.g.position))[0];
      if(hurt&&b.social>.62&&b.courage>.35&&b.alert<.5){this.target=hurt.g.position.clone().add(new THREE.Vector3(1,0,1));b.state='help';b.focus=hurt;return;}
      const body=npcs.filter(n=>n!==this&&n.hp<=0&&this.g.position.distanceTo(n.g.position)<12).sort((a,c)=>this.g.position.distanceTo(a.g.position)-this.g.position.distanceTo(c.g.position))[0];if(body&&b.curiosity>.65&&b.fear<.45){this.target=body.g.position.clone().add(new THREE.Vector3(2,0,2));b.state='observe';b.focus=body;return;}
      if(!this.target||this.g.position.distanceTo(this.target)<.8||Math.random()<.08)this.chooseWander();
    }
    tryMove(dir,dt,speed){const old=this.g.position.clone(),angles=[0,.55,-.55,1,-1];for(const a of angles){const c=Math.cos(a),s=Math.sin(a),v=new THREE.Vector3(dir.x*c-dir.z*s,0,dir.x*s+dir.z*c).normalize(),p=this.g.position.clone().addScaledVector(v,dt*speed);const road=isRoadPoint(p),blocked=hitsStatic(p,.36)||cars.some(car=>car!==this.ignoreCar&&!car.removed&&hitsCar(car,p,.38));if(!road&&!blocked){this.g.position.copy(p);this.g.rotation.y=Math.atan2(v.x,v.z);return true;}}this.g.position.copy(old);return false;}
    update(dt){
      if(this.hp<=0)return;this.forget(dt);this.ignoreCarTimer=Math.max(0,this.ignoreCarTimer-dt);if(this.ignoreCarTimer<=0)this.ignoreCar=null;
      if(this.down>0){this.down-=dt;const old=this.g.position.clone();this.g.position.addScaledVector(this.vel,dt);this.vel.y-=6*dt;this.g.position.y=Math.max(.18,this.g.position.y+this.vel.y*dt);this.vel.multiplyScalar(.96);const blockedByCar=cars.some(c=>c!==this.ignoreCar&&!c.removed&&hitsCar(c,this.g.position,.3));if(hitsStatic(this.g.position,.34)||blockedByCar)this.g.position.copy(old);if(this.down<=0){this.g.rotation.z=0;this.g.position.y=.18;this.vel.set(0,0,0);this.brain.state=this.hp<45?'flee':'wander';}return;}
      this.brain.think-=dt;if(this.brain.think<=0){this.brain.think=.32+Math.random()*.45;this.think();}
      if(!this.target){this.chooseWander();return;}const d=this.target.clone().sub(this.g.position).setY(0),dist=d.length();if(dist<.65){if(this.brain.state==='help'&&this.brain.focus?.hp>0)this.remember('helped',this.brain.focus.g.position,5,.4);this.target=null;this.brain.focus=null;return;}d.normalize();const pace=this.brain.state==='flee'||this.brain.state==='dodge'?3.2:this.brain.state==='help'?2:this.brain.state==='observe'?1.05:1.25;if(!this.tryMove(d,dt,pace)){this.target=null;this.brain.think=0;}
    }
  }
  ['Maya','Davi','Luna','Rico','Bia','Theo','Nina','Caio','Jade','Noah','Liz','Ivo'].forEach((n,i)=>npcs.push(new NPC(n,i)));
  function dropDriver(car,{dead=false,flee=false}={}){const d=car.driver;if(!d)return null;const worldPos=car.g.localToWorld(new THREE.Vector3(1.8,.18,0)),ped=new NPC('Motorista',0);ped.g.position.copy(worldPos);ped.g.scale.setScalar(1);ped.hp=dead?0:Math.max(1,d.userData.hp??100);ped.speed=flee?3.2:1.25;if(dead){ped.down=999;ped.g.rotation.z=Math.PI/2;}else if(flee){const away=worldPos.clone().sub(car.g.position).setY(0).normalize();ped.target=worldPos.clone().addScaledVector(away,18);ped.brain.state='flee';ped.remember('vehicle-danger',car.g.position,10,2);}car.g.remove(d);car.driver=null;car.ai=false;car.speed=0;return ped;}
  class Car{constructor(color,x,z,rot=0,ai=false,kind='normal'){Object.assign(this,carMesh(color,kind));this.g.position.set(x,.03,z);this.g.rotation.y=rot;scene.add(this.g);this.speed=0;this.hp=kind==='tow'?180:kind==='samu'?160:100;this.maxHp=this.hp;this.fuel=100;this.ai=ai;this.kind=kind;this.exploded=false;this.removed=false;this.route=null;this.idx=0;this.halfW=kind==='tow'?1.16:1.08;this.halfL=kind==='tow'?2.55:2.2;this.impactCooldown=0;this.driver=null;this.serviceJob=null;this.critical=false;this.criticalTimer=0;this.driverDecisionMade=false;cars.push(this);}assignRoad(){const horizontal=Math.random()<.5,lane=Math.random()<.5?-3.2:3.2;if(horizontal){const z=roads[Math.floor(Math.random()*3)]+lane;this.route=[new THREE.Vector3(-108,0,z),new THREE.Vector3(108,0,z)];}else{const x=roads[Math.floor(Math.random()*3)]+lane;this.route=[new THREE.Vector3(x,0,-108),new THREE.Vector3(x,0,108)];}this.idx=0;}damage(v){if(this.exploded||this.removed||this.critical)return;this.hp=Math.max(0,this.hp-v);if(this.driver&&!this.driverDecisionMade&&this.hp<=Math.max(18,this.maxHp*.18)){this.driverDecisionMade=true;if((this.driver.userData.hp??100)>0&&Math.random()<.75)dropDriver(this,{dead:false,flee:true});}if(this.hp<=0){this.critical=true;this.criticalTimer=3+Math.random()*2;this.ai=false;this.speed*=.4;this.body.material.color.setHex(0x3a2420);this.hood.material.color.setHex(0x452923);}}explode(){if(this.exploded||this.removed)return;this.exploded=true;this.critical=false;this.speed=0;this.ai=false;this.body.material.color.setHex(0x171616);this.hood.material.color.setHex(0x211b19);this.cab.material.color.setHex(0x202020);if(this.driver){this.driver.userData.hp=0;this.driver.rotation.z=Math.PI/2;}for(const n of npcs)if(n.hp>0&&n.g.position.distanceTo(this.g.position)<22){n.remember('explosion',this.g.position,14,2.3);n.brain.alert=1;}if(currentCar===this)leaveCar(true);if(this.kind!=='tow'&&this.kind!=='samu')towJobs.push(new TowJob(this));}isTowWreck(o){return this.serviceJob&&this.serviceJob.wreck===o&&['winch','position','return','park'].includes(this.serviceJob.state);}frontBlocked(){if(signalAhead(this))return true;const f=new THREE.Vector3(Math.sin(this.g.rotation.y),0,Math.cos(this.g.rotation.y));for(const o of cars){if(o===this||o.removed||this.isTowWreck(o))continue;const d=o.g.position.clone().sub(this.g.position).setY(0);if(d.length()<8&&f.dot(d.clone().normalize())>.72)return true;}for(const n of npcs){if(n.hp<=0)continue;const d=n.g.position.clone().sub(this.g.position).setY(0);if(d.length()<5&&f.dot(d.clone().normalize())>.75)return true;}return false;}move(dt){if(this.exploded||this.removed)return;this.impactCooldown=Math.max(0,this.impactCooldown-dt);const old=this.g.position.clone(),impact=Math.abs(this.speed),f=new THREE.Vector3(Math.sin(this.g.rotation.y),0,Math.cos(this.g.rotation.y));this.g.position.addScaledVector(f,this.speed*dt);const hit=carStaticHit(this);if(hit){const oldDist=Math.hypot(old.x-hit.x,old.z-hit.z),newDist=Math.hypot(this.g.position.x-hit.x,this.g.position.z-hit.z);if(this.speed<0&&newDist>oldDist+.001)return;this.g.position.copy(old);if((hit.type==='tree'||hit.type==='pole')&&impact>3){knock(hit.owner,this);if(this.impactCooldown<=0){this.damage(Math.max(2,impact));this.impactCooldown=.7;}this.speed*=.35;return;}if(this.impactCooldown<=0&&impact>1.25){this.damage(Math.max(2,impact*1.6));this.impactCooldown=.7;}this.speed=0;return;}for(const o of cars){if(o===this||o.removed||this.isTowWreck(o)||!carsTouch(this,o))continue;const oldDist=old.distanceTo(o.g.position),newDist=this.g.position.distanceTo(o.g.position);if(this.speed<0&&newDist>oldDist+.001)continue;this.g.position.copy(old);const rel=Math.max(impact,Math.abs(o.speed));if(!o.exploded&&rel>1.4){if(this.impactCooldown<=0){this.damage(Math.max(3,rel*1.55));this.impactCooldown=.7;}if(o.impactCooldown<=0){o.damage(Math.max(2,rel*1.2));o.impactCooldown=.7;}}this.speed=0;return;}if(!currentCar&&hitsCar(this,player.position,.28)&&impact>1.8){damagePlayerImpact(impact,f,this);this.speed*=.55;}for(const n of npcs)if(n.hp>0&&hitsCar(this,n.g.position,.28)){n.impact(impact,f,this);this.speed*=impact>6?.55:.8;}}update(dt){if(this.removed||this.exploded)return;if(this.critical){this.criticalTimer-=dt;this.speed*=Math.pow(.97,dt*60);if(this.criticalTimer<=0)this.explode();return;}if(this.ai){if(!this.driver||(this.driver.userData.hp??0)<=0){this.ai=false;this.speed=0;return;}if(!this.route)this.assignRoad();const t=this.route[this.idx],d=t.clone().sub(this.g.position).setY(0);if(d.length()<2){this.idx=1-this.idx;return;}const a=Math.atan2(d.x,d.z),delta=Math.atan2(Math.sin(a-this.g.rotation.y),Math.cos(a-this.g.rotation.y));this.g.rotation.y+=THREE.MathUtils.clamp(delta,-dt*.8,dt*.8);const target=this.frontBlocked()?0:5.2;this.speed=THREE.MathUtils.lerp(this.speed,target,Math.min(1,dt*2.4));this.move(dt);return;}if(currentCar!==this)return;if(this.fuel<=0){this.speed*=.97;return;}const gas=(keys.DriveGas||keys.KeyW)?1:0,reverse=(keys.DriveBrake||keys.KeyS)?1:0,st=(keys.DriveLeft||keys.KeyA?1:0)-(keys.DriveRight||keys.KeyD?1:0);if(gas&&!reverse)this.speed+=dt*9.5;if(reverse&&!gas){if(this.speed>1)this.speed-=dt*15;else this.speed-=dt*8.5;}if(!gas&&!reverse)this.speed*=Math.pow(.984,dt*60);this.speed=THREE.MathUtils.clamp(this.speed,-6,17);if(Math.abs(this.speed)>.2)this.g.rotation.y+=st*dt*1.35*Math.sign(this.speed);this.fuel=Math.max(0,this.fuel-dt*(.02+Math.abs(this.speed)*.01));this.move(dt);}}
  const parked=[new Car(0xc52b32,12,-4),new Car(0x2b668f,-52,8,Math.PI/2),new Car(0xd28a26,67,-8),new Car(0x34383e,-7,62,Math.PI/2)];for(let i=0;i<7;i++){const c=new Car([0x9f2f35,0x356a91,0xd28b28,0x44494f,0x2d7f63][i%5],-100+i*29,roads[i%3]+(i%2?3.2:-3.2),Math.PI/2,true);c.assignRoad();attachDriver(c,[0x36556f,0x5a4736,0x3d6044][i%3]);}
  const laneValues=roads.flatMap(r=>[r-3.2,r+3.2]);
  function nearestLaneInfo(p){let best=null,dist=Infinity;for(const x of laneValues){const q=new THREE.Vector3(x,.03,THREE.MathUtils.clamp(p.z,-108,108)),d=q.distanceToSquared(p);if(d<dist){dist=d;best={orientation:'v',coord:x,point:q};}}for(const z of laneValues){const q=new THREE.Vector3(THREE.MathUtils.clamp(p.x,-108,108),.03,z),d=q.distanceToSquared(p);if(d<dist){dist=d;best={orientation:'h',coord:z,point:q};}}return best;}
  function nearestRoadPoint(p){return nearestLaneInfo(p).point.clone();}
  function nearestLaneValue(v){return laneValues.reduce((a,b)=>Math.abs(b-v)<Math.abs(a-v)?b:a,laneValues[0]);}
  function pathBetween(from,to){const a=nearestLaneInfo(from),b=nearestLaneInfo(to),path=[a.point.clone()];if(a.orientation==='v'&&b.orientation==='h')path.push(new THREE.Vector3(a.coord,.03,b.coord));else if(a.orientation==='h'&&b.orientation==='v')path.push(new THREE.Vector3(b.coord,.03,a.coord));else if(a.orientation==='v'&&b.orientation==='v'&&Math.abs(a.coord-b.coord)>.1){const z=nearestLaneValue((a.point.z+b.point.z)/2);path.push(new THREE.Vector3(a.coord,.03,z),new THREE.Vector3(b.coord,.03,z));}else if(a.orientation==='h'&&b.orientation==='h'&&Math.abs(a.coord-b.coord)>.1){const x=nearestLaneValue((a.point.x+b.point.x)/2);path.push(new THREE.Vector3(x,.03,a.coord),new THREE.Vector3(x,.03,b.coord));}path.push(b.point.clone());return path.filter((p,i)=>i===0||p.distanceTo(path[i-1])>.35);}
  const TOW_BASE=new THREE.Vector3(-56.8,.03,-108),SAMU_BASE=new THREE.Vector3(63.2,.03,108);
  class TowJob{constructor(wreck){this.wreck=wreck;this.timer=8;this.state='waiting';this.tow=null;this.path=[];this.i=0;this.hook=0;this.service=null;this.stage=null;this.targetYaw=0;this.stuck=0;this.lastPos=null;this.replans=0;this.unstickTimer=0;this.resumeState='approach';}hitchDistance(){return (this.tow?.halfL||2.55)+(this.wreck?.halfL||2.2)+.45;}planService(from){this.service=nearestLaneInfo(this.wreck.g.position);const s=this.service.point.clone(),offset=this.hitchDistance()+2.8;let sign;if(this.service.orientation==='v'){sign=from.z<=s.z?-1:1;this.stage=s.clone().add(new THREE.Vector3(0,0,sign*offset));const reverseDir=s.clone().sub(this.stage).setY(0).normalize(),forward=reverseDir.multiplyScalar(-1);this.targetYaw=Math.atan2(forward.x,forward.z);}else{sign=from.x<=s.x?-1:1;this.stage=s.clone().add(new THREE.Vector3(sign*offset,0,0));const reverseDir=s.clone().sub(this.stage).setY(0).normalize(),forward=reverseDir.multiplyScalar(-1);this.targetYaw=Math.atan2(forward.x,forward.z);}this.path=pathBetween(from,this.stage);this.i=0;}spawn(){if(activeTowJob&&activeTowJob!==this)return false;activeTowJob=this;this.tow=new Car(0xe0b429,TOW_BASE.x,TOW_BASE.z,0,false,'tow');this.tow.serviceJob=this;attachDriver(this.tow,0x29455f);this.planService(TOW_BASE);this.state='approach';this.lastPos=this.tow.g.position.clone();this.stuck=0;return true;}release(){if(activeTowJob===this)activeTowJob=null;}recover(){this.stuck=0;this.replans++;if(!this.tow)return;this.tow.speed=0;this.unstickTimer=1.1;this.resumeState=this.state==='return'?'return':'approach';this.state='unstick';}monitor(dt){if(!this.tow||['waiting','position','winch','park','unstick'].includes(this.state))return;if(!this.lastPos)this.lastPos=this.tow.g.position.clone();const moved=this.tow.g.position.distanceTo(this.lastPos);if(moved<.025&&Math.abs(this.tow.speed)<.45)this.stuck+=dt;else this.stuck=Math.max(0,this.stuck-dt*2);this.lastPos.copy(this.tow.g.position);if(this.stuck>3.5)this.recover();}driveTo(target,dt,max=4.2){if(!target)return true;const d=target.clone().sub(this.tow.g.position).setY(0);if(d.length()<1.25){this.tow.speed=0;return true;}const a=Math.atan2(d.x,d.z),delta=Math.atan2(Math.sin(a-this.tow.g.rotation.y),Math.cos(a-this.tow.g.rotation.y));this.tow.g.rotation.y+=THREE.MathUtils.clamp(delta,-dt*.75,dt*.75);this.tow.speed=THREE.MathUtils.lerp(this.tow.speed,this.tow.frontBlocked()?0:max,Math.min(1,dt*2.2));this.tow.move(dt);return false;}update(dt){if(this.state==='done'||this.state==='stolen')return;if(this.state==='waiting'){if(activeTowJob&&activeTowJob!==this)return;this.timer-=dt;if(this.timer<=0)this.spawn();return;}if(!this.tow||this.tow.removed||this.tow.exploded){this.release();if(!this.wreck.removed){this.state='waiting';this.timer=10;this.tow=null;}else this.state='done';return;}if(currentCar===this.tow){this.state='stolen';this.release();const wreck=this.wreck;setTimeout(()=>{if(!wreck.removed)towJobs.push(new TowJob(wreck));},10000);return;}this.monitor(dt);if(this.state==='unstick'){this.unstickTimer-=dt;this.tow.speed=-1.1;this.tow.g.rotation.y+=dt*.45;this.tow.move(dt);if(this.unstickTimer<=0){this.tow.speed=0;if(this.resumeState==='return'){this.path=pathBetween(this.tow.g.position,TOW_BASE);this.i=0;this.state='return';}else{this.planService(this.tow.g.position);this.state='approach';}}return;}if(this.state==='approach'){if(this.driveTo(this.path[this.i],dt,4.1)){this.i++;if(this.i>=this.path.length){this.tow.speed=0;this.state='align';}}return;}if(this.state==='align'){const delta=Math.atan2(Math.sin(this.targetYaw-this.tow.g.rotation.y),Math.cos(this.targetYaw-this.tow.g.rotation.y));this.tow.g.rotation.y+=THREE.MathUtils.clamp(delta,-dt*.9,dt*.9);if(Math.abs(delta)<.05){this.tow.g.rotation.y=this.targetYaw;this.state='reverse';this.stuck=0;}return;}if(this.state==='reverse'){const d=this.service.point.clone().sub(this.tow.g.position).setY(0);if(d.length()<=1.35){this.tow.speed=0;this.state='winch';this.hook=3.5;return;}this.tow.speed=-1.15;this.tow.move(dt);return;}if(this.state==='winch'){this.tow.speed=0;this.hook-=dt;const back=new THREE.Vector3(-Math.sin(this.tow.g.rotation.y),0,-Math.cos(this.tow.g.rotation.y)),anchor=this.tow.g.position.clone().addScaledVector(back,this.hitchDistance()-.5);this.wreck.g.position.lerp(anchor,Math.min(1,dt*1.2));this.wreck.g.rotation.y=THREE.MathUtils.lerp(this.wreck.g.rotation.y,this.tow.g.rotation.y,Math.min(1,dt*1.5));if(this.hook<=0){this.wreck.g.position.copy(anchor);this.wreck.g.rotation.y=this.tow.g.rotation.y;this.state='return';this.path=pathBetween(this.tow.g.position,TOW_BASE);this.i=0;this.stuck=0;}return;}if(this.state==='return'){const arrived=this.driveTo(this.path[this.i],dt,3.4),back=new THREE.Vector3(-Math.sin(this.tow.g.rotation.y),0,-Math.cos(this.tow.g.rotation.y));this.wreck.g.position.copy(this.tow.g.position).addScaledVector(back,this.hitchDistance()-.5);this.wreck.g.rotation.y=this.tow.g.rotation.y;if(arrived){this.i++;if(this.i>=this.path.length){scene.remove(this.wreck.g);this.wreck.removed=true;this.state='park';this.timer=3;}}return;}if(this.state==='park'){this.timer-=dt;if(this.timer<=0){this.tow.removed=true;scene.remove(this.tow.g);this.release();this.state='done';}}}}
  function requestSamu(npc){if(!npc||npc.hp<=0||npc.samuRequested)return;npc.samuRequested=true;samuJobs.push(new SamuJob(npc));}
  class SamuJob{constructor(patient){this.patient=patient;this.state='waiting';this.timer=3;this.amb=null;this.path=[];this.i=0;this.paramedic=null;this.treat=0;}spawn(){if(activeSamuJob&&activeSamuJob!==this)return false;activeSamuJob=this;this.amb=new Car(0xf4f4f4,SAMU_BASE.x,SAMU_BASE.z,Math.PI,false,'samu');attachDriver(this.amb,0xffffff);this.path=pathBetween(SAMU_BASE,this.patient.g.position);this.i=0;this.state='approach';return true;}release(){if(activeSamuJob===this)activeSamuJob=null;}driveTo(target,dt,max=5){const d=target.clone().sub(this.amb.g.position).setY(0);if(d.length()<1.5){this.amb.speed=0;return true;}const a=Math.atan2(d.x,d.z),delta=Math.atan2(Math.sin(a-this.amb.g.rotation.y),Math.cos(a-this.amb.g.rotation.y));this.amb.g.rotation.y+=THREE.MathUtils.clamp(delta,-dt*.85,dt*.85);this.amb.speed=THREE.MathUtils.lerp(this.amb.speed,this.amb.frontBlocked()?0:max,Math.min(1,dt*2.5));this.amb.move(dt);return false;}cleanup(){if(this.paramedic)scene.remove(this.paramedic);if(this.amb){this.amb.removed=true;scene.remove(this.amb.g);}if(this.patient)this.patient.samuRequested=false;this.release();this.state='done';}update(dt){if(this.state==='done')return;if(this.patient.hp<=0){this.cleanup();return;}if(this.state==='waiting'){if(activeSamuJob&&activeSamuJob!==this)return;this.timer-=dt;if(this.timer<=0)this.spawn();return;}if(!this.amb||this.amb.removed||this.amb.exploded){this.release();this.patient.samuRequested=false;this.state='done';return;}if(this.state==='approach'){if(this.driveTo(this.path[this.i],dt,5)){this.i++;if(this.i>=this.path.length){this.state='deploy';this.timer=1;}}return;}if(this.state==='deploy'){this.amb.speed=0;this.timer-=dt;if(this.timer<=0){this.paramedic=person(0xf2f2f2,0xb98262);this.paramedic.position.copy(this.amb.g.position).add(new THREE.Vector3(1.6,.18,0));scene.add(this.paramedic);this.state='walk';}return;}if(this.state==='walk'){const d=this.patient.g.position.clone().sub(this.paramedic.position).setY(0);if(d.length()<.8){this.state='treat';this.treat=4;return;}d.normalize();this.paramedic.position.addScaledVector(d,dt*1.8);this.paramedic.rotation.y=Math.atan2(d.x,d.z);return;}if(this.state==='treat'){this.treat-=dt;if(this.treat<=0){this.patient.hp=Math.max(this.patient.hp,70);this.patient.down=0;this.patient.g.rotation.z=0;this.patient.g.position.y=.18;this.patient.samuRequested=false;this.state='return';this.path=pathBetween(this.amb.g.position,SAMU_BASE);this.i=0;scene.remove(this.paramedic);this.paramedic=null;}return;}if(this.state==='return'&&this.driveTo(this.path[this.i],dt,5)){this.i++;if(this.i>=this.path.length)this.cleanup();}}}
  const player=person(0x171b21,0xc98e6c);scene.add(player);const pistol=new THREE.Group();pistol.add(box(.13,.17,.7,mat(0x17191c,.35,.35),0,0,.35),box(.16,.35,.18,mat(0x25282c,.55,.2),0,-.2,.1));pistol.position.set(.35,1.38,.2);player.add(pistol);
  let health=100,money=1250,bandages=3,slot='fist',aiming=false,crouched=false,prone=false,running=false,jumpY=0,jumpV=0,dead=false,currentCar=null,lastShot=-999,reloading=false,mag=12,reserve=72,playerDown=0;const playerImpactVel=new THREE.Vector3(),MAG_SIZE=12,keys={},clock=new THREE.Clock(),joy={x:0,y:0,tx:0,ty:0,p:null};let playerIgnoreCar=null,playerIgnoreCarTimer=0,yaw=Math.PI,pitch=.12,camDist=5.8,drag=false,last={x:0,y:0},camPointer=null;
  const statusEl=$('#status'),moneyEl=$('#money'),healthEl=$('#health-value'),healthBar=$('#health-bar'),promptEl=$('#prompt'),vehicleHud=$('#vehicle-hud'),speedEl=$('#vehicle-speed'),fuelEl=$('#vehicle-fuel'),fuelBar=$('#vehicle-fuel-bar'),carHpEl=$('#vehicle-health'),carHpBar=$('#vehicle-health-bar'),ammoHud=$('#ammo-hud'),ammoMagEl=$('#ammo-mag'),ammoReserveEl=$('#ammo-reserve'),reloadBtn=$('#action-reload'),footUI=$('#on-foot-controls'),carUI=$('#car-controls'),crosshair=$('#crosshair');
  function cleanPoint(p,r=.48,ignore=null){return !hitsStatic(p,r)&&cars.every(c=>c===ignore||c.removed||!hitsCar(c,p,r));}
  function safeSpawn(){for(const [x,z] of [[12,12],[-12,12],[12,-12],[-12,-12],[0,14],[14,0]]){const p=new THREE.Vector3(x,.18,z);if(cleanPoint(p,.55)){player.position.copy(p);return;}}player.position.set(0,.18,14);}safeSpawn();
  function nearestVehicle(){return cars.filter(c=>!c.exploded&&!c.removed).map(car=>({car,d:car.g.position.distanceTo(player.position)})).sort((a,b)=>a.d-b.d)[0];}
  function leaveCar(force=false){if(!currentCar)return;const old=currentCar;currentCar=null;if(old.driver)old.driver.visible=true;player.visible=true;let placed=false;for(const [x,z] of [[2.7,0],[-2.7,0],[0,3.2],[0,-3.2]]){const p=old.g.localToWorld(new THREE.Vector3(x,.18,z));p.y=.18;if(cleanPoint(p,.48,old)){player.position.copy(p);placed=true;break;}}if(!placed)safeSpawn();footUI?.classList.remove('hidden');carUI?.classList.add('hidden');vehicleHud?.classList.add('hidden');keys.DriveGas=keys.DriveBrake=keys.DriveLeft=keys.DriveRight=false;if(force)health=Math.max(1,health-35);}
  function toggleCar(){if(currentCar){leaveCar();return;}const n=nearestVehicle();if(n&&n.d<4.8){currentCar=n.car;if(currentCar.driver)currentCar.driver.visible=false;currentCar.ai=false;currentCar.speed=0;player.visible=false;footUI?.classList.add('hidden');carUI?.classList.remove('hidden');vehicleHud?.classList.remove('hidden');aiming=false;refreshActionLabels();}}
  function damagePlayerImpact(impact,dir,sourceCar=null){if(dead)return;let dmg=0,push=.4,down=1.2;if(impact<4.5){dmg=5+impact*2;push=.7;down=1.2;}else if(impact<8){dmg=15+(impact-4.5)*6;push=2.9;down=2.2;}else{dmg=Math.min(95,35+(impact-8)*7);push=5.1;down=3.2;}if(sourceCar){playerIgnoreCar=sourceCar;playerIgnoreCarTimer=.85;const sep=separationPoint(sourceCar,player.position,.7);if(!hitsStatic(sep,.36))player.position.copy(sep);}health=Math.max(0,health-Math.round(dmg));playerDown=Math.max(playerDown,down);player.rotation.z=Math.PI/2;playerImpactVel.copy(dir).normalize().multiplyScalar(push);if(health<=0){dead=true;playerDown=0;setTimeout(()=>{health=100;dead=false;player.rotation.z=0;playerImpactVel.set(0,0,0);playerIgnoreCar=null;playerIgnoreCarTimer=0;safeSpawn();},2200);}}
  function useBandage(){if(dead||currentCar||bandages<=0||health>=100)return;bandages--;health=Math.min(100,health+35);slot='bandage';setTimeout(()=>{slot='fist';refreshActionLabels();},500);refreshActionLabels();}
  function cycleSlot(){slot=slot==='fist'?'pistol':slot==='pistol'?'bandage':'fist';aiming=false;refreshActionLabels();}
  function refreshActionLabels(){const s=$('#slot-name');if(s)s.textContent=slot==='fist'?'SOCO':slot==='pistol'?'PISTOLA':`BANDAGEM ${bandages}`;const a=$('#action-fire');if(a)a.textContent=slot==='fist'?'GOLPE':slot==='pistol'?'ATIRAR':'USAR';crosshair?.classList.toggle('hidden',slot!=='pistol'||!aiming||!!currentCar);pistol.visible=slot==='pistol'&&!currentCar&&!dead;ammoHud?.classList.toggle('hidden',slot!=='pistol'||!!currentCar);if(ammoMagEl)ammoMagEl.textContent=String(mag).padStart(2,'0');if(ammoReserveEl)ammoReserveEl.textContent=String(reserve).padStart(2,'0');}
  function reload(){if(reloading||slot!=='pistol'||currentCar||mag>=MAG_SIZE||reserve<=0)return;reloading=true;if(reloadBtn)reloadBtn.textContent='RECARREGANDO';setTimeout(()=>{const take=Math.min(MAG_SIZE-mag,reserve);mag+=take;reserve-=take;reloading=false;if(reloadBtn)reloadBtn.textContent='RECARREGAR';refreshActionLabels();},950);}
  function fireProjectile(){if(mag<=0){reload();return;}mag--;for(const n of npcs)if(n.hp>0&&n.g.position.distanceTo(player.position)<24){n.remember('gunshot',player.position,10,2);n.brain.alert=Math.max(n.brain.alert,.8);}const dir=new THREE.Vector3(-Math.sin(yaw)*Math.cos(pitch),-Math.sin(pitch),-Math.cos(yaw)*Math.cos(pitch)).normalize(),origin=player.position.clone().add(new THREE.Vector3(0,crouched?1.25:1.65,0)).addScaledVector(dir,.7),mesh=new THREE.Mesh(new THREE.SphereGeometry(.06,8,6),new THREE.MeshBasicMaterial({color:0xffd45c}));mesh.position.copy(origin);scene.add(mesh);projectiles.push({mesh,vel:dir.multiplyScalar(48),life:1.7});refreshActionLabels();}
  function updateProjectiles(dt){for(let i=projectiles.length-1;i>=0;i--){const p=projectiles[i];p.mesh.position.addScaledVector(p.vel,dt);p.life-=dt;let hit=false;for(const n of npcs){if(n.hp<=0)continue;const head=n.g.position.clone().add(new THREE.Vector3(0,2,0)),torso=n.g.position.clone().add(new THREE.Vector3(0,1.05,0));if(head.distanceTo(p.mesh.position)<.34){n.hit(120,'head');hit=true;break;}if(torso.distanceTo(p.mesh.position)<.78){n.hit(42,'body');hit=true;break;}}if(!hit)for(const c of cars){if(c.removed)continue;if(hitsCar(c,p.mesh.position,.05)){if(c.driver&&c.driver.visible!==false&&(c.driver.userData.hp??100)>0){const lp=c.g.worldToLocal(p.mesh.position.clone());if(Math.abs(lp.x)<.72&&lp.z>-1.25&&lp.z<.55&&lp.y>.65&&lp.y<2){const headshot=lp.y>1.45;c.driver.userData.hp=Math.max(0,(c.driver.userData.hp??100)-(headshot?120:55));if(c.driver.userData.hp<=0)dropDriver(c,{dead:true,flee:false});}}if(!c.exploded)c.damage(10);hit=true;break;}}if(!hit&&hitsStatic(p.mesh.position,.08))hit=true;if(hit||p.life<=0){scene.remove(p.mesh);projectiles.splice(i,1);}}}
  function primaryAction(){if(currentCar||dead)return;if(slot==='bandage'){useBandage();return;}if(slot==='fist'){const n=npcs.filter(n=>n.hp>0).map(n=>({n,d:n.g.position.distanceTo(player.position)})).sort((a,b)=>a.d-b.d)[0];if(n&&n.d<1.7)n.n.hit(15,'body');return;}if(reloading||clock.elapsedTime-lastShot<.22)return;lastShot=clock.elapsedTime;fireProjectile();}
  function jump(){if(currentCar||dead||prone||jumpY>0)return;jumpV=4.5;}
  function movePlayer(dt){if(currentCar||dead)return;playerIgnoreCarTimer=Math.max(0,playerIgnoreCarTimer-dt);if(playerIgnoreCarTimer<=0)playerIgnoreCar=null;if(playerDown>0){playerDown-=dt;const old=player.position.clone();player.position.addScaledVector(playerImpactVel,dt);playerImpactVel.multiplyScalar(Math.pow(.9,dt*60));const blockedByCar=cars.some(c=>c!==playerIgnoreCar&&!c.removed&&hitsCar(c,player.position,.35));if(hitsStatic(player.position,.42)||blockedByCar)player.position.copy(old);if(playerDown<=0){player.rotation.z=0;playerImpactVel.set(0,0,0);}return;}joy.x=THREE.MathUtils.lerp(joy.x,joy.tx,Math.min(1,dt*16));joy.y=THREE.MathUtils.lerp(joy.y,joy.ty,Math.min(1,dt*16));const x=Math.abs(joy.x)>.08?joy.x:(keys.KeyD?1:0)-(keys.KeyA?1:0),y=Math.abs(joy.y)>.08?joy.y:(keys.KeyW?1:0)-(keys.KeyS?1:0),m=Math.min(1,Math.hypot(x,y));running=!!keys.MobileRun;const f=new THREE.Vector3(-Math.sin(yaw),0,-Math.cos(yaw)),r=new THREE.Vector3(-f.z,0,f.x);if(m>.05){const mv=f.multiplyScalar(y/m).add(r.multiplyScalar(x/m)).normalize(),speed=(prone?1.2:crouched?2.1:running?7:4.1)*(.35+.65*m),delta=mv.clone().multiplyScalar(speed*dt),px=player.position.clone().add(new THREE.Vector3(delta.x,0,0)),pz=player.position.clone().add(new THREE.Vector3(0,0,delta.z));if(cleanPoint(px,.42))player.position.x=px.x;if(cleanPoint(pz,.42))player.position.z=pz.z;player.rotation.y=Math.atan2(mv.x,mv.z);}if(jumpY>0||jumpV>0){jumpV-=9*dt;jumpY=Math.max(0,jumpY+jumpV*dt);if(jumpY===0)jumpV=0;}player.position.y=.18+jumpY;player.scale.y=prone?.34:crouched?.72:1;}
  function updateCamera(){if(currentCar){const p=currentCar.g.localToWorld(new THREE.Vector3(0,1.55,.1)),l=currentCar.g.localToWorld(new THREE.Vector3(0,1.45,10));camera.position.copy(p);camera.lookAt(l);return;}if(slot==='pistol'&&aiming){const head=player.position.clone().add(new THREE.Vector3(0,prone?.65:crouched?1.2:1.72,0)),dir=new THREE.Vector3(-Math.sin(yaw)*Math.cos(pitch),-Math.sin(pitch),-Math.cos(yaw)*Math.cos(pitch));camera.position.copy(head);camera.lookAt(head.clone().add(dir.multiplyScalar(20)));return;}const target=player.position.clone().add(new THREE.Vector3(0,prone?.6:crouched?1:1.45,0)),h=Math.cos(pitch)*camDist;camera.position.copy(target).add(new THREE.Vector3(Math.sin(yaw)*h,Math.sin(pitch)*camDist+.8,Math.cos(yaw)*h));camera.lookAt(target);}
  function updateHud(){if(statusEl)statusEl.textContent=currentCar?'DIRIGINDO':playerDown>0?'CAÍDO':prone?'DEITADO':crouched?'AGACHADO':running?'CORRENDO':'A PÉ';if(moneyEl)moneyEl.textContent=money.toLocaleString('pt-BR');if(healthEl)healthEl.textContent=`${Math.round(health)}/100`;if(healthBar)healthBar.style.width=`${health}%`;if(currentCar){if(speedEl)speedEl.textContent=`${Math.round(Math.abs(currentCar.speed)*8)} km/h`;if(fuelEl)fuelEl.textContent=`${Math.round(currentCar.fuel)}%`;if(fuelBar)fuelBar.style.width=`${currentCar.fuel}%`;if(carHpEl)carHpEl.textContent=currentCar.critical?'CRÍTICO':`${Math.round(currentCar.hp)}%`;if(carHpBar)carHpBar.style.width=`${Math.max(0,currentCar.hp/currentCar.maxHp*100)}%`;}const n=nearestVehicle();if(promptEl)promptEl.textContent=!currentCar&&n&&n.d<4.8?'ENTRAR NO VEÍCULO':'';if(ammoMagEl)ammoMagEl.textContent=String(mag).padStart(2,'0');if(ammoReserveEl)ammoReserveEl.textContent=String(reserve).padStart(2,'0');}
  function sanitizeWorld(){for(const n of npcs){const p=n.g.position,onRoad=isRoadPoint(p);if(hitsStatic(p,.4)||onRoad)n.relocate();}for(const c of cars){if(!c.ai||c.kind!=='normal'||c.removed||c.exploded)continue;c.g.position.copy(nearestRoadPoint(c.g.position));c.speed=0;if(!c.driver)attachDriver(c);}}sanitizeWorld();
  addEventListener('keydown',e=>{keys[e.code]=true;if(e.repeat)return;if(e.code==='KeyE')toggleCar();if(e.code==='KeyF')cycleSlot();if(e.code==='KeyR')reload();if(e.code==='Space')jump();});addEventListener('keyup',e=>keys[e.code]=false);
  renderer.domElement.addEventListener('pointerdown',e=>{if(e.pointerType==='touch'&&e.clientX<innerWidth*.45)return;drag=true;camPointer=e.pointerId;last={x:e.clientX,y:e.clientY};});renderer.domElement.addEventListener('pointermove',e=>{if(!drag||e.pointerId!==camPointer)return;const dx=e.clientX-last.x,dy=e.clientY-last.y;yaw-=dx*.003;pitch=THREE.MathUtils.clamp(pitch+dy*.002,-.35,.55);last={x:e.clientX,y:e.clientY};});renderer.domElement.addEventListener('pointerup',()=>drag=false);renderer.domElement.addEventListener('pointercancel',()=>drag=false);
  $('#action-fire')?.addEventListener('pointerdown',e=>{e.preventDefault();primaryAction();});$('#action-slot')?.addEventListener('pointerdown',e=>{e.preventDefault();cycleSlot();});$('#action-aim')?.addEventListener('pointerdown',e=>{e.preventDefault();if(slot==='pistol'){aiming=!aiming;refreshActionLabels();}});$('#action-bandage')?.addEventListener('pointerdown',e=>{e.preventDefault();useBandage();});reloadBtn?.addEventListener('pointerdown',e=>{e.preventDefault();reload();});$('#action-crouch')?.addEventListener('pointerdown',e=>{e.preventDefault();crouched=!crouched;prone=false;});$('#action-prone')?.addEventListener('pointerdown',e=>{e.preventDefault();prone=!prone;crouched=false;});$('#action-run')?.addEventListener('pointerdown',e=>{e.preventDefault();keys.MobileRun=!keys.MobileRun;});$('#action-jump')?.addEventListener('pointerdown',e=>{e.preventDefault();jump();});$('#action-use')?.addEventListener('pointerdown',e=>{e.preventDefault();toggleCar();});$('#car-exit')?.addEventListener('pointerdown',e=>{e.preventDefault();leaveCar();});
  const joyEl=$('#joystick'),knob=$('#joystick-knob');if(joyEl&&knob){function jm(e){const r=joyEl.getBoundingClientRect(),cx=r.left+r.width/2,cy=r.top+r.height/2;let dx=e.clientX-cx,dy=e.clientY-cy,max=Math.min(r.width,r.height)*.3,l=Math.hypot(dx,dy)||1;if(l>max){dx=dx/l*max;dy=dy/l*max;}const nx=dx/max,ny=-dy/max,magv=Math.hypot(nx,ny);if(magv<.12)joy.tx=joy.ty=0;else{const s=(magv-.12)/.88;joy.tx=nx/magv*s;joy.ty=ny/magv*s;}knob.style.transform=`translate(${dx}px,${dy}px)`;}joyEl.onpointerdown=e=>{e.preventDefault();joy.p=e.pointerId;joyEl.setPointerCapture?.(e.pointerId);jm(e);};joyEl.onpointermove=e=>{if(joy.p===e.pointerId)jm(e);};const je=e=>{if(joy.p!==e.pointerId)return;joy.p=null;joy.tx=joy.ty=0;knob.style.transform='translate(0,0)';};joyEl.onpointerup=je;joyEl.onpointercancel=je;}
  for(const b of document.querySelectorAll('[data-drive]')){const k={left:'DriveLeft',right:'DriveRight',gas:'DriveGas',brake:'DriveBrake'}[b.dataset.drive],on=e=>{e.preventDefault();keys[k]=true;},off=e=>{e.preventDefault();keys[k]=false;};b.onpointerdown=on;b.onpointerup=off;b.onpointercancel=off;b.onpointerleave=off;}
  function resizeGame(){const v=viewport();camera.aspect=v.w/v.h;camera.updateProjectionMatrix();renderer.setSize(v.w,v.h,false);}addEventListener('resize',resizeGame);addEventListener('orientationchange',()=>setTimeout(resizeGame,120));refreshActionLabels();
  let firstFrame=false;function loop(){requestAnimationFrame(loop);const dt=Math.min(clock.getDelta(),.045),t=clock.elapsedTime;updateSignals(t);movePlayer(dt);for(const c of cars)c.update(dt);for(const n of npcs)n.update(dt);for(const o of [...trees,...poles])if(o.fallen){const a=o.sign*Math.PI*.48;if(o.axis==='x')o.g.rotation.x=THREE.MathUtils.lerp(o.g.rotation.x,a,dt*3);else o.g.rotation.z=THREE.MathUtils.lerp(o.g.rotation.z,a,dt*3);}for(const j of towJobs)j.update(dt);for(const s of samuJobs)s.update(dt);updateProjectiles(dt);updateCamera();updateHud();renderer.render(scene,camera);if(!firstFrame){firstFrame=true;setTimeout(()=>$('#loading-screen')?.classList.add('done'),250);console.info('[Cidade Viva] boot OK',VERSION);}}loop();
} catch (error) { showFatal(error); }
